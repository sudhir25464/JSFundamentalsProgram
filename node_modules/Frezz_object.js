const { writeFile } = require("fs")
const { Writable } = require("stream")

let user = {
    name : 'Rama kumar',
    adharnumber :1245673456,
    hasBankaccount :true

}


Object.defineProperty(user,adharnumber,{
    writable:false
})

console.log(user);